import { PagePlaceholder } from '../../components/ui/PagePlaceholder'

export function NewOrderPage() {
  return (
    <PagePlaceholder
      title="Nueva orden"
      description="Formulario para crear una orden de producción compartida. Wireframe del flujo de alta sin backend real."
    />
  )
}
